mutation UpdateOnePlaylist($playlistId: String!, $playlistPayload: PlaylistPayloadInput!) {
  updateOnePlaylist(input: {
    playlistId: $playlistId
    playlistPayload: $playlistPayload
  }) {
    _id
    name
  }
}